# Handoff — begin RepoBrain issue implementation

## Goal

Begin implementing the approved RepoBrain issue sequence. The user reports that the foundation PR [#8](https://github.com/devintucker24/trell/pull/8) is merged.

## Source of truth

- Parent specification: [GitHub issue #10](https://github.com/devintucker24/trell/issues/10)
- Approved tracer tickets: [#11–#22](https://github.com/devintucker24/trell/issues/10)
- Earlier planning artifact (background only): `/opt/cursor/artifacts/plans/wiki_sim_then_docs_a75971cc.plan.md.plan.md`

Do not re-derive the spec from this handoff; read the relevant issue body and its blockers.

## Current frontier

Start with [#11 — Automate baseline evaluation](https://github.com/devintucker24/trell/issues/11). It has no blockers and must prove the existing system before later implementation.

Then follow the dependency sequence:

1. #11 baseline evaluation
2. #21 corpus vs `_system/` architecture
3. #12 RepoBrain identity and technical CLI
4. #22 Graphify adapter hardening
5. Downstream inventory/retrieval/conversion/cheat-sheet/dashboard tickets according to their `Blocked by` sections
6. #20 compatibility contraction last

Important: [#21](https://github.com/devintucker24/trell/issues/21) explicitly supersedes the parent specification’s earlier statement that `_system/` reorganization was out of scope.

## Settled architecture constraints

- Product name: **RepoBrain**.
- Keep `docs/wiki/` as the host knowledge root.
- Move portable engine/config/generated machinery under `docs/wiki/_system/` through expand–migrate–contract.
- Keep `graphify-out/` separate and Graphify-owned.
- Graphify is the only code AST/call graph; RepoBrain wraps it but does not rebuild it.
- Semantic corpus pages are agent-authored through triage/ingest and human-reviewed, not generated as truth.
- Existing ADRs/docs sites/context files are discovered as raw sources, not copied into semantic folders.
- Dashboard is generated, local, read-only HTML with commands and agent prompts; no mutation server or agent authentication.
- MarkItDown safe local conversion is planned after raw inventory/retrieval; external/OCR/media/cloud paths are opt-in.

## Repository start procedure

1. Fetch latest `origin/main` because PR #8 has now merged.
2. Confirm #11 is still open and unassigned; read its full body/comments.
3. Create a fresh issue-specific feature branch from latest `main` using the required `cursor/<name>-ec23` pattern.
4. Implement only #11’s acceptance criteria.
5. Run the relevant Python/wiki checks and `cargo test`.
6. Commit, push, and create/update the PR according to repository instructions.

Do not bundle #21 or later tickets into #11. The evaluation must first measure the foundation as it exists.

## Suggested skills

- `implement` or `implement-spec` — execute issue #11 from its acceptance criteria.
- `wiki-brain` / `wiki-retrieve` — current pre-rename RepoBrain operator and retrieval playbooks.
- `wiki-doctor` — structural validation.
- `cargo-verify` — final Rust repository verification.
- `handoff` — if #11 cannot be completed in the next session.

## Operational notes

- GitHub Issues write permission now works; #10–#22 were published and labeled `ready-for-agent`.
- The prior PR merge conflicts were mechanical duplicate-history conflicts, resolved and verified before PR #8 merged.
- No RepoBrain child issue implementation has started in this conversation.

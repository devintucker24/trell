# RepoBrain configuration

`HOST.yaml` and `router-seeds.md` are host-owned configuration. The remaining
files are portable engine contracts. Operators resolve this directory through
`repobrain_paths.py`; callers should not derive it themselves.
